spring:
  datasource:
    url: jdbc:h2:file:./data/local/fracturecare;MODE=MySQL;DATABASE_TO_LOWER=TRUE;AUTO_SERVER=TRUE
    username: sa
    password: ""
    driver-class-name: org.h2.Driver

server:
  port: 8081

app:
  frontend-origin: http://127.0.0.1:5173
  security:
    jwt-secret: ${APP_JWT_SECRET}
  ai:
    mode: mock
